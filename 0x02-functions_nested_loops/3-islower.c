#include "main.h"

/**
 * int _islower - function that checks for lowercase character.
 *
 * return: 0.
 */
int _islower(int c)
{
	char c;

	if (c >= 'a' && >= 'z')
	{
		return (1);
	}
	else 
	{
		return (0);
	}
}
